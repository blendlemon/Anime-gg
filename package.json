{
  "name": "anime-openings-tournament",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npx pnpm install && npx pnpm build",
    "start": "NODE_ENV=production node server/src/index.js",
    "postinstall": "cd server && npx pnpm install"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
